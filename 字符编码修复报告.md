# 字符编码乱码修复报告

## 问题描述
Windows 终端显示中文乱码，例如：
```
鍔犺浇axis.xml鏂囦欢鎴愬姛
```

## 原因分析
- 源代码文件使用 **UTF-8** 编码
- Windows 终端默认使用 **GBK** 编码
- 当 UTF-8 编码的中文字符串在 GBK 终端显示时，会出现乱码

## 解决方案

### ✅ 方案1：将所有中文字符串改为英文（已实施）

这是最彻底的解决方案，避免了编码问题，同时提高了代码的国际化程度。

#### 修复的文件和内容

1. **include/common/xmlParsing.h**
   ```cpp
   // 修复前
   std::cout << "加载" << xmlName << "文件成功" << std::endl;
   throw std::runtime_error("没有找到工程名 zrcs");
   throw std::runtime_error("读取xml文件失败");
   
   // 修复后
   std::cout << "Successfully loaded " << xmlName << " file" << std::endl;
   throw std::runtime_error("Project 'zrcs' not found in path");
   throw std::runtime_error("Failed to read XML file");
   ```

2. **include/controller/virtual/Coppeliasim.h**
   ```cpp
   // 修复前
   std::cout << "成功获取句柄：" << jointNames_[i] << " -> " << jointHandles_[i] << std::endl;
   throw std::runtime_error("远程API调用失败,错误码: " + std::to_string(ret));
   throw std::runtime_error("错误：无法获取关节" + std::to_string(returnCode));
   throw std::runtime_error("无法连接到 CoppeliaSim 服务器");
   std::cerr << "设置关节位置失败，错误码: " << ret << std::endl;
   std::cerr << "无效的从站ID: " << slaveId_ << std::endl;
   
   // 修复后
   std::cout << "Successfully got handle: " << jointNames_[i] << " -> " << jointHandles_[i] << std::endl;
   throw std::runtime_error("Remote API call failed, error code: " + std::to_string(ret));
   throw std::runtime_error("Error: Unable to get joint handle, error code: " + std::to_string(returnCode));
   throw std::runtime_error("Unable to connect to CoppeliaSim server");
   std::cerr << "Failed to set joint position, error code: " << ret << std::endl;
   std::cerr << "Invalid slave ID: " << slaveId_ << std::endl;
   ```

3. **include/controller/axisConfig.h**
   ```cpp
   // 修复前
   throw std::runtime_error("读取轴参数失败");
   
   // 修复后
   throw std::runtime_error("Failed to read axis parameters");
   ```

4. **include/common/sharedMemory/rtProcess.h**
   ```cpp
   // 修复前
   // 创建共享内存和 SharedBlock 对象
   // 禁用拷贝构造和赋值
   
   // 修复后
   // Create shared memory and SharedBlock object
   // Disable copy constructor and assignment
   ```

---

## 修复统计

| 文件 | 修复数量 | 类型 |
|------|---------|------|
| xmlParsing.h | 3处 | 输出/异常 |
| Coppeliasim.h | 6处 | 输出/异常 |
| axisConfig.h | 1处 | 异常 |
| rtProcess.h | 2处 | 注释 |
| **总计** | **12处** | **全部修复** |

---

## 其他可选方案（未实施）

### 方案2：修改终端编码为 UTF-8
在 Windows 终端执行：
```cmd
chcp 65001
```
**缺点**：每次打开终端都需要执行，不够方便。

### 方案3：在代码中设置控制台编码
```cpp
#ifdef _WIN32
#include <windows.h>
SetConsoleOutputCP(CP_UTF8);
#endif
```
**缺点**：需要修改主函数，且可能影响其他输出。

### 方案4：使用宽字符
```cpp
std::wcout << L"加载" << xmlName << L"文件成功" << std::endl;
```
**缺点**：需要大量修改代码，且跨平台兼容性问题。

---

## 建议

### ✅ 已采用的最佳实践
1. **使用英文字符串** - 避免编码问题，提高国际化
2. **保留中文注释** - 方便中文开发者理解代码
3. **统一编码规范** - 所有源文件使用 UTF-8 编码

### 📝 代码规范建议
1. **日志输出**：使用英文，便于国际化和问题排查
2. **注释**：可以使用中文，方便团队理解
3. **变量名**：使用英文，符合编程规范
4. **错误信息**：使用英文，便于搜索和调试

---

## 验证

修复后，终端输出应该显示：
```
Successfully loaded axis.xml file
```

而不是乱码：
```
鍔犺浇axis.xml鏂囦欢鎴愬姛
```

---

## 总结

✅ 所有中文输出字符串已改为英文  
✅ 避免了 Windows 终端乱码问题  
✅ 提高了代码的国际化程度  
✅ 保持了中文注释便于理解  

**修复完成时间**: 2026-03-07  
**修复人员**: AI Code Assistant  
**状态**: ✅ 完成
